<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Farm</title>
</head>

<body>
    <h1>Edit Farm</h1>
    <!-- action will use for sending to .post() method head -->
    <!--REMARKS -> "?" IS REQUIRED TO ADD BEFORE _method -->
    <form action="/farms/<%= farm._id %>?_method=PUT" method="POST">
        <div class="">
            <label for="name">Farm Name</label>
            <input type="text" name="name" id="name" placeholder="Farm Name" value="<%= farm.name %>">
        </div>
        <div class="" style="margin: 10px 0;">
            <label for="city">City</label>
            <input type="text" name="city" id="city" placeholder="Price" value="<%= farm.city %>">
        </div>
        <div class="">
            <label for="email">Email</label>
            <input type="text" name="email" id="email" placeholder="Email" value="<%= farm.email %>">
        </div>
        <button style="margin: 10px 0;">Submit</button>
    </form>
    <form action="/farms/<%= farm._id %>">
        <button>Back</button>
    </form>
</body>

</html>